---
name: socratic-deep-questioning
description: 苏格拉底式深度提问法。面对一个模糊的人生/情感/决策问题，不急着给答案，而是每次只问一个精准问题，逐层深挖关键维度，直到收集到足够上下文，再输出一份结构化评估（概率判断 + 挑战假设 + 可执行方向）。触发词：用苏格拉底提问法、一次只问一个问题、问我直到你理解、帮我分析这件事的可能性。反例：纯知识问答/定义查询/技术 how-to 不应触发。
---

# 苏格拉底式深度提问法 (Socratic Deep Questioning)

## 一句话定义

面对一个模糊的人生/决策问题，不急着给答案，而是每次只问一个精准问题，逐层深挖，直到收集到足够上下文，再输出一份结构化评估。

## 1. Trigger conditions（触发条件）

满足以下任一即触发：

- 用户明确说"用苏格拉底提问法""一次只问一个问题""问我直到你理解"
- 用户抛出一个模糊的个人/情感/决策问题，且附带"帮我分析/判断可能性"
- 问题本身涉及"概率/可行性评估"，但关键变量都缺失，直接回答会是空谈

**反例（不应触发）：** 纯知识问答、定义查询、技术 how-to——这些直接回答即可，不需要追问。

## 2. Input schema（输入）

| 字段 | 必填 | 说明 |
|------|------|------|
| topic | 是 | 用户想搞清楚的核心话题 |
| probe_until | 是 | 停止追问的信号（默认："直到你认为完全理解"） |
| target_output | 可选 | 最终想要评估/建议/概率判断 |

## 3. Output schema（输出）

分两阶段产出：

### 阶段 A — 追问阶段（多轮）

```
单条问题 + 简短承接上一轮答案 + 说明这轮问题为什么重要
```

### 阶段 B — 综合评估（一轮）

```
用户画像总结 + 目标画像总结
↓
诚实结论（概率/可行性判断）
↓
挑战关键假设
↓
可执行方向
```

## 4. Execution SOP（执行流程）

1. **拆维度**：默想这个话题需要哪些维度才能判断（动机、自身条件、渠道、短板、标准、认知盲区等）
2. **每次只问一个**：从最底层的"为什么"开始，每轮只抛一个精准问题
3. **承接不评判**：每轮先用一两句接住对方答案，不带褒贬，然后抛下一个
4. **标记假设**：发现对方回答里隐含的因果链/假设时，先口头记一句"这个先放放，后面聊"
5. **终止判断**：当各关键维度都有答案、足以做评估时，停止追问
6. **综合**：先拼出双方画像 → 给概率/结论 → 逐个挑战之前标记的假设 → 给方向
7. **量化辅助**：当结论涉及概率/规模时，用漏斗图等可视化让抽象变具体

## 5. Guardrails（护栏）

- 一次只问一个问题，不堆问题
- 在收集够上下文前，禁止"先给答案再补问"——先理解再下结论
- 不对用户答案做道德评判，但可以挑战事实假设
- 不说教、不灌鸡汤、不用"你很棒"这类无效鼓励
- 挑战假设时要直白但不刻薄，承接时要短不要变成复读
- 综合结论必须诚实，不为了好看而美化数字
- 评估样例中不含任何可识别用户身份的具体细节（脱敏）

## 6. Eval examples（评估示例）— 已脱敏

### 样例 A（标准流程）

- **输入 topic：** 某人想判断"自己能否达成一个目标人群/关系"，但关键变量全部缺失
- **追问轮次：** 7 轮，覆盖维度依次为：动机 → 自身条件 → 渠道 → 经验 → 外在 → 标准 → 社交能力
- **关键假设标记：** 1 处（用户对目标人群的某个因果假设，最终被挑战并修正）
- **输出：** 漏斗式概率可视化 + 区间判断（维持现状约极低，主动改变后约中等偏低）+ 分阶段行动方向

### 样例 B（边界反例）

- **输入 topic：** "Python 和 Java 哪个更适合写后端"
- **应触发：** 否。这是知识/技术对比题，变量基本公开，直接给答案即可，不需要逐层追问。

### 判定"做对了"的 checklist

- [ ] 每轮只有一个问题，没有堆叠
- [ ] 收集完上下文才下结论，没有提前剧透
- [ ] 最终结论至少挑战了用户最初的 1 个隐含假设
- [ ] 结论附带可执行方向，而非空泛建议
- [ ] 量化结论时用了可视化让抽象变具体
- [ ] 不含任何可识别用户身份的具体细节（脱敏通过）
