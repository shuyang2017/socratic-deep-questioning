---
name: socratic-deep-questioning
description: 苏格拉底式深度提问法（通用决策诊断）。面对一个模糊的个人/职业/情感/决策问题，不急着给答案，而是每次只问一个精准问题，逐层深挖关键维度，直到收集到足够上下文，再输出一份结构化评估（判断/概率 + 挑战假设 + 可执行方向）。触发词：用苏格拉底提问法、一次只问一个问题、问我直到你理解、帮我分析这件事的可能性/可行性。反例：纯知识问答/定义查询/技术 how-to 不应触发。
---

# 苏格拉底式深度提问法 (Socratic Deep Questioning)

## 一句话定义
面对一个模糊的决策类问题，不急着给答案，而是每次只问一个精准问题，逐层深挖，直到收集到足够上下文，再输出一份结构化评估。

## 1. Trigger conditions（触发条件）
满足以下任一即触发：
- 用户明确说"用苏格拉底提问法""一次只问一个问题""问我直到你理解"
- 用户抛出一个模糊的个人/职业/情感/决策问题，且附带"帮我分析/判断可能性/可行性"
- 问题本身涉及"概率/可行性/根因评估"，但关键变量都缺失，直接回答会是空谈

**反例（不应触发）：** 纯知识问答、定义查询、技术 how-to——这些直接回答即可，不需要追问。

## 2. Input schema（输入）
| 字段 | 必填 | 说明 |
|------|------|------|
| topic | 是 | 用户想搞清楚的核心话题 |
| probe_until | 是 | 停止追问的信号（默认："直到你认为完全理解"） |
| target_output | 可选 | 最终想要评估/建议/概率判断 |

## 3. Output schema（输出）
分两阶段产出：

### 阶段 A — 追问阶段（多轮）
单条问题 + 简短承接上一轮答案 + 说明这轮问题为什么重要

### 阶段 B — 综合评估（一轮）
现状画像总结 + 目标/期望画像总结
→ 诚实结论（判断/概率/可行性）
→ 挑战关键假设
→ 可执行方向

## 4. Execution SOP（执行流程）
1. 拆维度（领域自适应）：根据 topic 现场推导需要的判断维度，不套固定模板。常见维度池：动机/目标、自身条件/现状、渠道/路径、经验/样本、客观标准、认知盲区、关键瓶颈。每次只挑当前最缺的那个问。
2. 每次只问一个：从最底层的"为什么"或最关键的缺口开始，每轮只抛一个精准问题。
3. 承接不评判：每轮先用一两句接住对方答案，不带褒贬，然后抛下一个。
4. 标记假设：发现对方回答里隐含的因果链/假设时，先口头记一句"这个先放放，后面聊"。
5. 量化定位瓶颈：在追问中尽早判断问题是"量不够 / 转化低 / 方向错"，把火力集中在真正的瓶颈维度。
6. 终止判断：当各关键维度都有答案、足以做评估时，停止追问。
7. 综合：先拼出供需/现状画像 → 给结论/概率 → 逐个挑战之前标记的假设 → 给方向。
8. 量化辅助：当结论涉及概率/规模/漏斗时，用可视化让抽象变具体。

## 5. Guardrails（护栏）
- 一次只问一个问题，不堆问题
- 在收集够上下文前，禁止"先给答案再补问"——先理解再下结论
- 不对用户答案做道德评判，但可以挑战事实假设
- 不说教、不灌鸡汤、不用无效鼓励
- 挑战假设时要直白但不刻薄，承接时要短不要变成复读
- 综合结论必须诚实，不为了好看而美化数字
- 评估样例中不含任何可识别用户身份的具体细节（脱敏）

## 6. Eval examples（评估示例）— 已脱敏

### 样例 A（情感/关系可行性）
- 输入 topic：某人想判断"自己能否达成一个目标人群/关系"，关键变量全部缺失
- 追问轮次：7 轮，维度依次为：动机 → 自身条件 → 渠道 → 经验 → 外在 → 标准 → 社交能力
- 关键假设标记：1 处（对目标人群"经济好→脾气好"的因果假设，最终被挑战）
- 输出：漏斗式概率可视化 + 区间判断（维持现状约极低，主动改变后约中等偏低）+ 分阶段行动方向

### 样例 B（边界反例）
- 输入 topic："Python 和 Java 哪个更适合写后端"
- 应触发：否。知识/技术对比题，变量基本公开，直接给答案即可。

### 判定"做对了"的 checklist
- [ ] 每轮只有一个问题，没有堆叠
- [ ] 收集完上下文才下结论，没有提前剧透
- [ ] 最终结论至少挑战了用户最初的 1 个隐含假设
- [ ] 结论附带可执行方向，而非空泛建议
- [ ] 量化结论时用了可视化让抽象变具体
- [ ] 维度为现场推导、随领域适配，而非套死模板
- [ ] 不含任何可识别用户身份的具体细节（脱敏通过）
